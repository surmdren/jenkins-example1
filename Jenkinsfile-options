pipeline {
    agent any
    stages {
        stage('Example') {
            options {
                timeout(time: 1, unit: 'HOURS') 
                buildDiscarder(logRotator(numToKeepStr: '1'))
            }
            steps {
                echo 'Hello World'
            }
        }
    }
}