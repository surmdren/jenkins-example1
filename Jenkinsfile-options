pipeline {
    agent any
    options {
        buildDiscarder(logRotator(numToKeepStr: '1', artifactNumToKeepStr: '30'))
    }
    stages {
        stage('Example') {
            steps {
                echo 'Hello World'
            }
        }
    }
}