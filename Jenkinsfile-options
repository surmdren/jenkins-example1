pipeline {
    agent any
    stages {
        stage('Example') {
            options {
                buildDiscarder(logRotator(numToKeepStr: '1'))
            }
            steps {
                echo 'Hello World'
            }
        }
    }
}