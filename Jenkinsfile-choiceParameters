pipeline {
    agent any
    stages {
        stage('choiceParameters') {
            steps {
              script{
                newChoice  = [$class: 'ChoiceParameterDefinition', choices: ['','ADD','SHOW','DELETE','UPDATE','RESET'], description: 'Choose something', name: ''];
              }
            }
        }
    }
}